{
  "name": "gaurav-singh.info",
  "version": "1.0.0",
  "description": "Personal website",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --watch",
    "clean": "rm -rf _site",
    "cv:pdf": "npm run build && node scripts/generate-cv-pdf.mjs && node scripts/generate-cv-pdf.mjs src/assets/gaurav-singh-academic-cv.pdf /cv/academic/",
    "start": "npm run dev",
    "test": "node --test"
  },
  "author": "Mathscapes",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "gray-matter": "^4.0.3"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0"
  }
}
