            
            <!-- Cite as -->
            <div class="cite-as">
                <!--<p><em>Cite this page as:</em> {{ metadata.author }}. <b>{{ title }}.</b> <i>The Personal Website of Gaurav Singh</i>. Last modified {{ page.date | postDate }}. <a href="{{ metadata.url }}{{ page.url }}">{{ metadata.url }}{{ page.url }}</a></p>-->
                <a id="copyLink" onclick="copyToClipboard(event)" href="#">Cite this webpage</a>
                <script>
                function copyToClipboard(event) {
                    event.preventDefault();
                    const citationText = `{{ metadata.author }}. "{{ title }}." The Personal Website of Gaurav Singh. Last modified {{ page.date | postDate }}. {{ metadata.url }}{{ page.url }}`;
                    const copyLink = document.getElementById('copyLink');
                    const originalScrollPosition = window.pageYOffset;
                    navigator.clipboard.writeText(citationText).then(() => {
                        copyLink.textContent = 'Citation copied';
                        setTimeout(() => {
                            copyLink.textContent = 'Cite this page';
                            window.scrollTo(0, originalScrollPosition);
                        }, 3000);
                    }).catch(err => {
                        console.error('Failed to copy: ', err);
                        window.scrollTo(0, originalScrollPosition);
                    });
                }
                </script>
            </div> &nbsp;