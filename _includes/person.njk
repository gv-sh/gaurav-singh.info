<!DOCTYPE html>
<html lang="en">
    {% include "head.njk" %}
    <body>

        <!-- Header -->
        {% include "header.njk" %}

        <!-- Sidebar -->
        {% include "sidebar.njk" %}

        <!-- Main content -->
        <main>
            <div class="item">
                <h3>{{ title }}</h3>
                <h6>{{ attribution }}</h6>
            </div>
            
            {{ content | safe }}
        </main>

        <!-- Footer -->
        {% include "footer.njk" %}

    </body>

</html>