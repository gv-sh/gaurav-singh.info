---
layout: base.njk
---
{% include "intro.njk" %}

<article class="note">
  <h2>{{ title }}</h2>
  <p class="note-date"><time datetime="{{ date | isoDate }}">{{ date | blogDate }}</time></p>
  {% if description %}<p class="note-intro">{{ description }}</p>{% endif %}
  <div class="note-body">
    {{ content | safe }}
  </div>
</article>
